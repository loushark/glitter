<!DOCTYPE html>
<html>
  <head>
    <title>Glitter</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <header>
    <h1 id="page-title">٭Glitter٭</h1>
  </header>
  <nav>
    <%= link_to(image_tag("home.png"), root_path, id:"home-button" )%>
    <% if !logged_in? %>
    <h3>welcome Guest!</h3>
      <%= link_to "Sign Up", signup_path, id: "signup-button"%>
      <%= link_to "Login", login_path, id:"login-button" %>
    <%else%>
    <h3>welcome <%= current_user.username %>! </h3>
      <%= link_to "Logout", logout_path, id: "logout-button" %>
    <%end%>
  </nav>
  <body>
    <%= yield %>
  </body>
  <footer>
    <p>- brought to you by loushark productions ltd -</p>
  </footer>
</html>
